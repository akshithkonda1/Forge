"""ARIA, Bedrock/Claude, and the SimRunner harness.

The deployable coaching engine lives next to the Lambda it ships in
(``backend.infra.lambda`` / ``backend/infra/lambda/services/aria_engine.py``).
This package is the local/dev and evaluation surface for the same mind.
"""
